#include <stdio.h>
/**
 * main - entry point
 * description: a program that prints the lowercase alphabet in reverse
 * Return: Always 0 (success)
 */
int main(void)
{
char x;
for (x = 'z'; x >= 'a'; s--)
{
putchar(x);
}
putchar('\n');
return (0);
}
