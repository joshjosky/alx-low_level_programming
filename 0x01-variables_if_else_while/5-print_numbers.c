#include <stdio.h>
/**
 * main - entry point
 * description: a code that prints all single digit of base 10
 * Return: 0 always (success)
 */
int main(void)
{
int i;
for (i = 0; i < 10; i++)
{
printf("%d", i);
}
printf("\n");
return (0);
}
