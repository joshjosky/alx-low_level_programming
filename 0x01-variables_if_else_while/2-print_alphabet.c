#include <stdio.h>
#include <stdlib.h>
#include <time.h>
/**
 * main - entry point
 * Description: a program that prints the alphabet in lowercase
 * Return: 0 (success)
 */
int main(void)
{
char x;
x = 'a';
while (x <= 'z')
{
putchar(x);
c++;
}
putchar('\n');
return (0);
}
